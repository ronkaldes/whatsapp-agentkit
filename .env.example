# OpenAI API Key (obrigatório)
OPENAI_API_KEY=sk-proj-.............

# ID do Workflow do Agent Builder (obrigatório)
WORKFLOW_ID=wf_...............

# Usar workflow real do ChatKit (opcional, padrão: false)
# Se true: Tenta chamar o workflow real via /chatkit/workflows/{id}/runs
# Se false ou não definido: Usa chat completions simulando o workflow
USE_REAL_WORKFLOW=false

# Supabase Configuration (obrigatório para dashboard)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key